---
title: Heatmap Chart
description: Categorical heatmaps for session grids, correlation matrices, and intensity maps — powered by Apache ECharts
image: /og/heatmap-chart.png
links:
  github: https://github.com/ctesibius/beecharts/blob/main/src/registry/charts/heatmap-chart.tsx
  doc: https://echarts.apache.org/en/option.html#series-heatmap
  api: https://echarts.apache.org/en/option.html#series-heatmap
---

<ComponentPreview title="Basic Chart" name="ex-heatmap-chart"  />

## Installation

<CodeTabs>
  <TabsList variant="underline">
    <TabsTab value="cli">CLI</TabsTab>
    <TabsTab value="manual">Manual</TabsTab>
  </TabsList>
  <TabsPanel value="cli">
    <CliBlock commands={["@beecharts/heatmap-chart", "@beecharts/chart-recipes"]} />
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>
        <StepTitle>Install the following dependencies:</StepTitle>
        <StepContent>
          <CommandBlock commands={["echarts"]} />
        </StepContent>
      </Step>
      <Step>
        <StepTitle>Copy and paste the following code snippets into your project.</StepTitle>
         <StepDescription>
          To use the chart, first create the folder `beecharts` and a subfolder called `charts` inside your `components` directory.
          Then, copy the following base heatmap-chart code into a new file in that folder.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="heatmap-chart"
            title="components/beecharts/charts/heatmap-chart.tsx"
          />
        </StepContent>
      </Step>
       <Step>
        <StepTitle>Now, Let's add the chart component to your project.</StepTitle>
        <StepDescription>
          These Components are required by the chart component to render the chart. Make a folder called `ui` inside the `beecharts` folder and paste the code there.

          Below is main chart component.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="chart"
            title="components/beecharts/ui/chart.tsx"
          />
        </StepContent>
      </Step>
       <Step>
        <StepTitle>Add the chart-recipes helper.</StepTitle>
        <StepDescription>
          Copy `chart-recipes.ts` into your project (or install `@beecharts/chart-recipes`) so you can flatten row × column matrices with `prepareHeatmapCells()`.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="chart-recipes"
            title="lib/chart-recipes.ts"
          />
        </StepContent>
      </Step>
    </Steps>
  </TabsPanel>
</CodeTabs>

## Usage

The heatmap chart is composible. `<BeeHeatmapChart>` is the container — pass a `chartConfig` whose keys match your intensity series. Flatten a row × column matrix with `prepareHeatmapCells()`, then compose `<Heatmap />` with the resulting cells and optional axis labels. Add `<Tooltip />` and `<Legend />` when you want the shared BeeCharts chrome.

```tsx
import {
  BeeHeatmapChart,
  Heatmap,
  Tooltip,
  Legend,
} from "@/components/beecharts/charts/heatmap-chart";
import { prepareHeatmapCells } from "@/lib/chart-recipes";
```

```tsx
const rowLabels = ["Mon", "Tue", "Wed", "Thu", "Fri"];
const colLabels = ["9am", "12pm", "3pm", "6pm"];
const matrix = [
  [12, 28, 45, 22],
  [18, 35, 52, 30],
  [10, 22, 38, 18],
  [24, 42, 58, 36],
  [15, 30, 48, 26],
];

const { cells, min, max } = prepareHeatmapCells(rowLabels, colLabels, matrix);

const chartConfig = {
  intensity: {
    label: "Sessions",
    colors: {
      light: ["#fff7ed", "#f97316", "#9a3412"],
      dark: ["#431407", "#ea580c", "#fdba74"],
    },
  },
} satisfies ChartConfig;

<BeeHeatmapChart config={chartConfig} className="h-80 w-full">
  <Heatmap
    dataKey="intensity"
    data={cells}
    xLabels={colLabels}
    yLabels={rowLabels}
    min={min}
    max={max}
  />
  <Legend />
  <Tooltip />
</BeeHeatmapChart>
```

`prepareHeatmapCells()` returns flat `{ row, col, x, y, value }` cells plus computed `min` / `max` for the visual map. Pass explicit `min` and `max` when you need a fixed scale across refreshes (for example, comparing weeks).

## Examples

### Session grid

Weekday × time-of-day activity — the default preview uses a warm amber scale so low traffic stays quiet and peaks read clearly.

<ComponentPreview title="session grid" name="ex-heatmap-chart"  />

### Weekly density

A wider grid (seven days × eight slots) with a rose scale for dashboards that need finer time resolution.

<ComponentPreview title="weekly density" name="ex-heatmap-weekly-chart"  />

### Correlation matrix

Symmetric product × product scores — useful for affinity, co-purchase, or feature correlation views.

<ComponentPreview title="correlation matrix" name="ex-heatmap-correlation-chart"  />

### Team workload

Employee × day utilization for capacity planning — see also the [calendar chart](/docs/calendar-chart/static) for single-person month views.

<ComponentPreview title="team workload" name="ex-heatmap-team-workload-chart"  />

## API Reference

The chart is composed of a root container and declarative parts. Render the root, then compose the slots you need as children.

<ApiHeading>BeeHeatmapChart</ApiHeading>

The root container. Owns `chartConfig`, the ECharts canvas, and the part registry that compiles heatmap options.

<ApiTable>
  <ApiRow name="config" type="ChartConfig" required>
    Configuration for the intensity series. Use a three-stop `colors.light` / `colors.dark` array on the key passed to `<Heatmap dataKey />` — the compiler maps them to the ECharts visual map gradient.
  </ApiRow>
  <ApiRow name="children" type="ReactNode" required>
    Compose `<Heatmap />` (required for data), optionally `<Tooltip />` and `<Legend />`.
  </ApiRow>
  <ApiRow name="className" type="string">
    Size and layout classes on the chart container (for example `h-80 w-full`).
  </ApiRow>
</ApiTable>

<ApiHeading>Heatmap</ApiHeading>

Registers matrix cells and axis labels with the ECharts compiler. Renders no DOM of its own.

<ApiTable>
  <ApiRow name="dataKey" type="string" required>
    Key into `chartConfig` for the visual map gradient and tooltip labels.
  </ApiRow>
  <ApiRow name="data" type="HeatmapCell[]" required>
    Flat cells from `prepareHeatmapCells()` — each `{ row, col, x, y, value }`.
  </ApiRow>
  <ApiRow name="xLabels" type="string[]">
    Column category labels (x-axis). Inferred from unique `col` values when omitted.
  </ApiRow>
  <ApiRow name="yLabels" type="string[]">
    Row category labels (y-axis). Inferred from unique `row` values when omitted.
  </ApiRow>
  <ApiRow name="min" type="number">
    Visual map minimum. Defaults to the smallest cell `value`.
  </ApiRow>
  <ApiRow name="max" type="number">
    Visual map maximum. Defaults to the largest cell `value`.
  </ApiRow>
</ApiTable>

<ApiHeading>Tooltip</ApiHeading>

Enables the shared BeeCharts HTML tooltip layer when composed.

<ApiHeading>Legend</ApiHeading>

Hides the built-in ECharts legend and reserves space for the shared BeeCharts legend chrome when composed.
