---
title: Bar Chart
description: Simple static & beautifully designed bar charts
image: /og/bar-chart.png
links:
  github: https://github.com/ctesibius/nqchart/blob/main/src/registry/charts/bar-chart.tsx
  doc: https://echarts.apache.org/en/option.html#series-bar
  api: https://echarts.apache.org/en/option.html#series-bar
---

<ComponentPreview className="mb-0" title="Basic Chart" name="ex-bar-chart"  />
<ComponentPreview className="mb-0" title="stackType='stacked'" name="ex-stacked-type-bar-chart"  />
<ComponentPreview title="layout='horizontal'" name="ex-horizontal-layout-bar-chart"  />

## Installation

<CodeTabs>
  <TabsList variant="underline">
    <TabsTab value="cli">CLI</TabsTab>
    <TabsTab value="manual">Manual</TabsTab>
  </TabsList>
  <TabsPanel value="cli">
    <CliBlock commands={["@nqchart/bar-chart"]} />
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>
        <StepTitle>Install the following dependencies:</StepTitle>
        <StepContent>
          <CommandBlock commands={["echarts", "motion"]} />
        </StepContent>
      </Step>
      <Step>
        <StepTitle>Copy and paste the following code snippets into your project.</StepTitle>
         <StepDescription>
          To use the chart, first create the folder `nqchart` and a subfolder called `charts` inside your `components` directory.
          Then, copy the following base bar-chart code into a new file in that folder.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="bar-chart"
            title="components/nqchart/charts/bar-chart.tsx"
          />
        </StepContent>
      </Step>
       <Step>
        <StepTitle>Now, Let's add the chart component to your project.</StepTitle>
        <StepDescription>
          These Components are required by the chart component to render the chart. Make a folder called `ui` inside the `nqchart` folder and paste the code there.

          Below is main chart component.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="chart"
            title="components/nqchart/ui/chart.tsx"
          />
        </StepContent>
      </Step>
       <Step>
        <StepTitle>Now, We need to add sub components.</StepTitle>
        <StepDescription>
          Create a file called `tooltip.tsx` inside the `nqchart/ui` folder and paste the code there.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="tooltip"
            title="components/nqchart/ui/tooltip.tsx"
          />
        </StepContent>
        <StepDescription>
          Now, create another file called `legend.tsx` inside the `nqchart/ui` folder and paste the code there.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="legend"
            title="components/nqchart/ui/legend.tsx"
          />
        </StepContent>
      </Step>
    </Steps>
  </TabsPanel>
</CodeTabs>

## Usage

The bar chart is composible. `<NQBarChart>` is the container, and you compose only the parts you need — `<Grid>`, `<XAxis>`, `<YAxis>`, `<Legend>`, `<Tooltip>`, and one or more `<Bar>` — as its children. Each `<Bar>` carries its own `variant`, `radius`, `glowing`, `bufferBar`, and `isClickable`, so a single chart can mix fill styles and make only some series interactive.

```tsx
import {
  NQBarChart,
  Bar,
  XAxis,
  YAxis,
  Grid,
  Tooltip,
  Legend,
} from "@/components/nqchart/charts/bar-chart";
```

```tsx
const chartConfig = {
  desktop: {
    label: "Desktop",
    colors: { light: ["#047857"], dark: ["#10b981"] },
  },
  mobile: {
    label: "Mobile",
    colors: { light: ["#be123c"], dark: ["#f43f5e"] },
  },
} satisfies ChartConfig;

<NQBarChart data={data} config={chartConfig} xDataKey="month">
  <Grid />
  <XAxis dataKey="month" />
  <Legend />
  <Tooltip />
  <Bar dataKey="desktop" />
  <Bar dataKey="mobile" />
</NQBarChart>
```

Bar, line, and composed **basic** previews share the same twelve-month `desktop` / `mobile` dataset, color tokens, and **3px** rounded bar tops (`barRadius` on the chart root — nqui `--radius-sm` at 16px root).

### Loading State

<ComponentPreview className="mb-0" title="isLoading='true'" name="ex-loading-state-bar-chart"  />
<Alert > 
  <AlertContent >
    The bar chart supports loading state with a shimmer animation. You can pass the `isLoading` prop to the chart to show the loading state while your data is being fetched.
  </AlertContent>
</Alert>

## More examples

Below are bar chart examples with different `stackType`, `layout`, and `<Bar variant />` fills.

### Stack types

The stacked preview is at the top of this page. Below is the normalized 100% variant.

<ComponentPreview className="mb-0" title="stackType='percent'" name="ex-percent-type-bar-chart"  />

### Bar variants

<ComponentPreview className="mb-0" title="variant='hatched'" name="ex-hatched-variant-bar-chart"  />
<ComponentPreview title="variant='stripped'" name="ex-stripped-variant-bar-chart"  />

### 3D bar

Isometric bars with beveled faces and a highlighted peak — see [Bar blocks](/docs/bar-chart/blocks) for the full block.

<ComponentPreview title="Isometric 3D bar" name="isometric-bar-chart"  />

### Histogram

Bin numeric samples with `binForHistogram()` from [Chart recipes](/docs/chart-recipes), then render with `variant="histogram"` on `<NQBarChart />` (touching bins, square corners).

<ComponentPreview title="histogram" name="ex-histogram-chart"  />

<ComponentPreview className="mb-0" title="loading" name="ex-loading-state-histogram-chart"  />

### Bullet chart

Horizontal range bands plus actual and target markers — use `prepareBulletRow()` and `layout="horizontal"`.

<ComponentPreview title="bullet" name="ex-bullet-chart"  />

## API Reference

The chart is composed of several parts. The props below are grouped by the component they belong to.

<ApiHeading>NQBarChart</ApiHeading>

The root container. It owns the data, the shared selection state, the loading skeleton, and the optional brush. Everything visual is composed as its children.

<ApiTable>
  <ApiRow name="data" type="TData[]" required>
    Data used to display the chart. An array of objects where each object represents a data point (`TData extends Record<string, unknown>`).
  </ApiRow>
  <ApiRow name="config" type="Record<string, ChartConfig[string]>" required>
    Configuration object that defines the chart's series. Each key should match a data key in your data array, with a corresponding color or color array.
  </ApiRow>
  <ApiRow name="children" type="ReactNode" required>
    The composed chart parts — `<Grid />`, `<XAxis />`, `<YAxis />`, `<Legend />`, `<Tooltip />`, and one or more `<Bar />`.
  </ApiRow>
  <ApiRow name="className" type="string">
    Additional CSS classes to apply to the chart container.
  </ApiRow>
  <ApiRow name="stackType" type='"default" | "stacked" | "percent"' default='"default"'>
    Controls how multiple bars combine. `"default"` renders bars side by side, `"stacked"` stacks them on top of each other, and `"percent"` normalizes them to show percentage distribution.
  </ApiRow>
  <ApiRow name="layout" type='"vertical" | "horizontal"' default='"vertical"'>
    The orientation of the bars. `"vertical"` displays bars vertically, `"horizontal"` displays bars horizontally — in which case the `<YAxis />` shows categories and the `<XAxis />` shows values.
  </ApiRow>
  <ApiRow name="barRadius" type="number" default="3">
    The default corner radius for every `<Bar />`, in pixels. Each `<Bar />` may override it locally with its own `radius` prop.
  </ApiRow>
  <ApiRow name="animationType" type='"none" | "left-to-right" | "right-to-left" | "center-out" | "edges-in"' default='"left-to-right"'>
    Order in which bars grow into view, inherited by every `<Bar />`. Each bar grows from its baseline (bottom for vertical layout, left for horizontal). `"none"` disables it — devices with the OS reduce-motion preference fall back to `"none"` automatically.
  </ApiRow>
  <ApiRow name="barGap" type="number">
    The gap between bars in the same category (when using multiple series).
  </ApiRow>
  <ApiRow name="barCategoryGap" type="number">
    The gap between different categories of bars.
  </ApiRow>
  <ApiRow name="backgroundVariant" type="BackgroundVariant">
    Background pattern variant to display behind the chart.
  </ApiRow>
  <ApiRow name="defaultSelectedDataKey" type="string | null" default="null">
    The data key that should be selected by default.
  </ApiRow>
  <ApiRow name="onSelectionChange" type="(selectedDataKey: string | null) => void">
    Callback fired when a series is selected or deselected — by clicking a clickable `<Bar />` or `<Legend />` entry. Receives the selected data key, or `null` when deselected.
  </ApiRow>
  <ApiRow name="isLoading" type="boolean" default="false">
    Shows a loading skeleton animation with a shimmer effect when data is being fetched.
  </ApiRow>
  <ApiRow name="loadingBars" type="number" default="12">
    Number of bars to display in the loading skeleton.
  </ApiRow>
  <ApiRow name="showBrush" type="boolean" default="false">
    When enabled, displays a brush control below the chart for selecting and zooming into a range of data.
  </ApiRow>
  <ApiRow name="xDataKey" type="keyof TData & string">
    The data key used for the x-axis. Only needed by the brush footer — the axis itself reads its key from `<XAxis dataKey="…" />`.
  </ApiRow>
  <ApiRow name="brushHeight" type="number">
    The height of the brush preview area in pixels.
  </ApiRow>
  <ApiRow name="brushFormatLabel" type="(value: unknown, index: number) => string">
    Custom formatter for the brush axis labels.
  </ApiRow>
  <ApiRow name="onBrushChange" type="(range: NQBrushRange) => void">
    Callback invoked when the user changes the brush selection range.
  </ApiRow>
  <ApiRow name="chartProps" type="ComponentProps<typeof BarChart>">
    Additional ECharts options merged into the compiled `option` by NQChart. Read the <Link href="https://echarts.apache.org/en/option.html#series-bar" _blank>ECharts BarChart documentation</Link> for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>Bar</ApiHeading>

A single bar series. Each `<Bar />` is self-contained and generates its own gradient/pattern definitions, so a chart can hold any number of bars — each with its own variant, radius, glow, and clickability.

<ApiTable>
  <ApiRow name="dataKey" type="string" required>
    The series key. Must exist on both the data rows and the chart `config`.
  </ApiRow>
  <ApiRow name="variant" type='"default" | "hatched" | "duotone" | "duotone-reverse" | "gradient" | "stripped"' default='"default"'>
    The visual style of the bar fill. Applies to this bar only.
  </ApiRow>
  <ApiRow name="radius" type="number">
    The corner radius of this bar, in pixels. Falls back to the chart's `barRadius` when omitted.
  </ApiRow>
  <ApiRow name="animationType" type='"none" | "left-to-right" | "right-to-left" | "center-out" | "edges-in"'>
    The grow-in order for this bar series. Falls back to the chart's `animationType` when omitted.
  </ApiRow>
  <ApiRow name="isClickable" type="boolean" default="false">
    Lets this bar be selected by clicking it. When any bar is selected, unselected bars become semi-transparent.
  </ApiRow>
  <ApiRow name="enableHoverHighlight" type="boolean" default="false">
    When enabled, hovering over a bar dims this bar while another is hovered, making it easier to focus on specific data points.
  </ApiRow>
  <ApiRow name="glowing" type="boolean" default="false">
    Applies a soft outer glow to this bar series.
  </ApiRow>
  <ApiRow name="bufferBar" type="boolean" default="false">
    When enabled, renders this series' last data point with a diagonal lines (hatched) pattern while the rest stay solid. Useful for indicating projected or incomplete data at the end of a series.
  </ApiRow>
  <ApiRow name="barProps" type="ComponentProps<typeof Bar>">
    Escape hatch for raw ECharts series options compiled by NQChart.
  </ApiRow>
</ApiTable>

<ApiHeading>XAxis and YAxis</ApiHeading>

The category and value axes. Both ship with the chart's flat default styling and map NQChart axis props to ECharts axis options, so `dataKey`, `tickFormatter`, `tickMargin`, etc. pass straight through. They are hidden automatically while the chart is loading, and each resolves its axis `type` from the chart `layout` — categorical or numeric — unless you set `type` explicitly.

<ApiTable>
  <ApiRow name="dataKey" type="string">
    The data key for the axis values.
  </ApiRow>
  <ApiRow name="…axisProps">
    Additional axis options are merged into the compiled ECharts option. Read the <Link href="https://echarts.apache.org/en/option.html#xAxis" _blank>ECharts xAxis</Link> and <Link href="https://echarts.apache.org/en/option.html#yAxis" _blank>ECharts yAxis</Link> documentation for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>Grid</ApiHeading>

The background grid lines. Defaults to dashed lines aligned to the value axis based on the chart layout, and maps to ECharts grid options.

<ApiTable>
  <ApiRow name="…gridProps">
    Additional grid options are merged into the compiled ECharts option. Read the <Link href="https://echarts.apache.org/en/option.html#grid" _blank>ECharts grid documentation</Link> for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>Tooltip</ApiHeading>

The hover tooltip. It reads the chart's selection state so its content dims unselected series.

<ApiTable>
  <ApiRow name="variant" type='"default" | "frosted-glass"' default='"default"'>
    The visual style of the tooltip surface.
  </ApiRow>
  <ApiRow name="roundness" type='"sm" | "md" | "lg" | "xl"' default='"lg"'>
    Controls the border-radius of the tooltip.
  </ApiRow>
  <ApiRow name="defaultIndex" type="number">
    When set, the tooltip is visible by default at the specified data point index.
  </ApiRow>
</ApiTable>

<ApiHeading>Legend</ApiHeading>

The series legend. When `isClickable` is set, each entry toggles selection of its series.

<ApiTable>
  <ApiRow name="variant" type='"square" | "circle" | "circle-outline" | "rounded-square" | "rounded-square-outline" | "vertical-bar" | "horizontal-bar"'>
    The visual style of the legend indicators.
  </ApiRow>
  <ApiRow name="align" type='"left" | "center" | "right"' default='"right"'>
    Horizontal placement of the legend.
  </ApiRow>
  <ApiRow name="verticalAlign" type='"top" | "middle" | "bottom"' default='"top"'>
    Vertical placement of the legend.
  </ApiRow>
  <ApiRow name="isClickable" type="boolean" default="false">
    Lets each legend entry toggle selection of its series.
  </ApiRow>
</ApiTable>
