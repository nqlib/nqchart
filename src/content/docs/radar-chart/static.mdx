---
title: Radar Chart
description: Beautiful radar charts with filled and lines variants, gradient colors, and glow effects
image: /og/radar-chart.png
links:
  github: https://github.com/ctesibius/nqchart/blob/main/src/registry/charts/radar-chart.tsx
  doc: https://echarts.apache.org/en/option.html#series-radar
  api: https://echarts.apache.org/en/option.html#series-radar
---

<ComponentPreview title="Basic Chart" name="ex-radar-chart"  />

## Installation

<CodeTabs>
  <TabsList variant="underline">
    <TabsTab value="cli">CLI</TabsTab>
    <TabsTab value="manual">Manual</TabsTab>
  </TabsList>
  <TabsPanel value="cli">
    <CliBlock commands={["@nqchart/radar-chart"]} />
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>
        <StepTitle>Install the following dependencies:</StepTitle>
        <StepContent>
          <CommandBlock commands={["echarts"]} />
        </StepContent>
      </Step>
      <Step>
        <StepTitle>Copy and paste the following code snippets into your project.</StepTitle>
         <StepDescription>
          To use the chart, first create the folder `nqchart` and a subfolder called `charts` inside your `components` directory.
          Then, copy the following base radar-chart code into a new file in that folder.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="radar-chart"
            title="components/nqchart/charts/radar-chart.tsx"
          />
        </StepContent>
      </Step>
       <Step>
        <StepTitle>Now, Let's add the chart component to your project.</StepTitle>
        <StepDescription>
          These Components are required by the chart component to render the chart. Make a folder called `ui` inside the `nqchart` folder and paste the code there.

          Below is main chart component.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="chart"
            title="components/nqchart/ui/chart.tsx"
          />
        </StepContent>
      </Step>
       <Step>
        <StepTitle>Now, We need to add sub components.</StepTitle>
        <StepDescription>
          Create a file called `tooltip.tsx` inside the `nqchart/ui` folder and paste the code there.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="tooltip"
            title="components/nqchart/ui/tooltip.tsx"
          />
        </StepContent>
        <StepDescription>
          Now, create another file called `legend.tsx` inside the `nqchart/ui` folder and paste the code there.
        </StepDescription>
        <StepContent>
          <ComponentSource
            name="legend"
            title="components/nqchart/ui/legend.tsx"
          />
        </StepContent>
      </Step>
    </Steps>
  </TabsPanel>
</CodeTabs>

## Usage

The radar chart is a composible compound component. `<NQRadarChart />` is the root container — every visual part (`<PolarGrid />`, `<PolarAngleAxis />`, `<Tooltip />`, `<Legend />`, and the `<Radar />` series) is composed as a child, so you render exactly what you need.

```tsx
import {
  NQRadarChart,
  Radar,
  PolarGrid,
  PolarAngleAxis,
  Tooltip,
  Legend,
  Dot,
  ActiveDot,
} from "@/components/nqchart/charts/radar-chart";
```

```tsx
const data = [
  { skill: "JavaScript", desktop: 186, mobile: 80 },
  { skill: "TypeScript", desktop: 305, mobile: 200 },
  { skill: "React", desktop: 237, mobile: 120 },
  { skill: "Node.js", desktop: 173, mobile: 190 },
  { skill: "CSS", desktop: 209, mobile: 130 },
];

const chartConfig = {
  desktop: {
    label: "Desktop",
    colors: { light: ["#3b82f6"], dark: ["#60a5fa"] },
  },
  mobile: {
    label: "Mobile",
    colors: { light: ["#10b981"], dark: ["#34d399"] },
  },
} satisfies ChartConfig;

<NQRadarChart data={data} config={chartConfig}>
  <PolarGrid />
  <PolarAngleAxis dataKey="skill" />
  <Legend />
  <Tooltip />
  <Radar dataKey="desktop" variant="filled">
    <Dot variant="colored-border" />
    <ActiveDot variant="default" />
  </Radar>
  <Radar dataKey="mobile" variant="filled" />
</NQRadarChart>
```

### Interactive Selection

Set `isClickable` on a `<Radar />` to let it be selected by clicking, and on `<Legend />` to let legend entries toggle selection. Use the root's `onSelectionChange` callback to react to selection events:

```tsx
<NQRadarChart
  data={data}
  config={chartConfig}
  onSelectionChange={(selectedDataKey) => {
    if (selectedDataKey) {
      console.log("Selected:", selectedDataKey);
    } else {
      console.log("Deselected");
    }
  }}
>
  <PolarGrid />
  <PolarAngleAxis dataKey="skill" />
  <Legend isClickable />
  <Tooltip />
  <Radar dataKey="desktop" variant="filled" isClickable />
  <Radar dataKey="mobile" variant="filled" isClickable />
</NQRadarChart>
```

### Loading State

<ComponentPreview className="mb-0" title="isLoading='true'" name="ex-loading-state-radar-chart"  />
<Alert > 
  <AlertContent >
    The radar chart supports loading state with animated data. You can pass the `isLoading` prop to the root to show a loading animation while your data is being fetched.
  </AlertContent>
</Alert>

```tsx
<NQRadarChart data={[]} config={chartConfig} isLoading>
  <PolarGrid />
  <PolarAngleAxis dataKey="skill" />
  <Legend />
  <Tooltip />
  <Radar dataKey="desktop" variant="filled" />
  <Radar dataKey="mobile" variant="filled" />
</NQRadarChart>
```

## Examples

Below are some examples of the radar chart with different configurations.

### Lines Variant

<ComponentPreview className="mb-0" title="variant='lines'" name="ex-lines-variant-radar-chart"  />
<Alert> 
  <AlertContent>
    Set `variant="lines"` to show only the outline without fill. This is useful for comparing multiple datasets more clearly.
  </AlertContent>
</Alert>

### Circle Grid

<ComponentPreview className="mb-0" title="gridType='circle'" name="ex-circle-grid-radar-chart"  />
<Alert> 
  <AlertContent>
    Set `gridType="circle"` to use circular grid lines instead of the default polygon grid.
  </AlertContent>
</Alert>

### Gradient Colors

<ComponentPreview title="gradient colors" name="ex-gradient-colors-radar-chart"  />

### Glowing Radars

<ComponentPreview className="mb-0" title="<Radar isGlowing />" name="ex-glowing-radar-chart"  />
<Alert> 
  <AlertContent>
    Add a subtle glow effect to a radar by setting `isGlowing` on its `<Radar />`. Each radar controls its own glow independently.
  </AlertContent>
</Alert>


## API Reference

The radar chart is composed of a root container and a set of composible parts. Each part is documented in its own section below.

<ApiHeading>NQRadarChart</ApiHeading>

The root container. Owns the data, the shared context, and the loading skeleton. All other parts must be rendered as its children.

<ApiTable>
  <ApiRow name="data" type="TData[]" required>
    Data used to display the chart. An array of objects where each object represents a data point on the radar (`TData extends Record<string, unknown>`).
  </ApiRow>
  <ApiRow name="config" type="Record<string, ChartConfig[string]>" required>
    Configuration object that defines the chart's radar series. Each key should match a numeric data key in your data array, with corresponding colors and labels.
  </ApiRow>
  <ApiRow name="children" type="ReactNode" required>
    The composed parts of the chart — `<PolarGrid />`, `<PolarAngleAxis />`, `<PolarRadiusAxis />`, `<Tooltip />`, `<Legend />`, and one or more `<Radar />` series.
  </ApiRow>
  <ApiRow name="className" type="string">
    Additional CSS classes to apply to the chart container.
  </ApiRow>
  <ApiRow name="backgroundVariant" type="BackgroundVariant">
    Background pattern variant to display behind the chart.
  </ApiRow>
  <ApiRow name="defaultSelectedDataKey" type="string | null" default="null">
    The radar series selected on first render.
  </ApiRow>
  <ApiRow name="onSelectionChange" type="(selectedDataKey: string | null) => void">
    Callback fired when a radar is selected or deselected. Receives the selected data key, or `null` when deselected.
  </ApiRow>
  <ApiRow name="isLoading" type="boolean" default="false">
    Shows a loading animation with animated data when data is being fetched.
  </ApiRow>
  <ApiRow name="loadingPoints" type="number" default="6">
    Number of points rendered in the loading skeleton radar.
  </ApiRow>
  <ApiRow name="chartProps" type="ComponentProps<typeof RadarChart>">
    Additional ECharts options merged into the compiled `option` by NQChart. Read the <Link href="https://echarts.apache.org/en/option.html#series-radar" _blank>ECharts RadarChart documentation</Link> for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>Radar</ApiHeading>

A single radar series. Each `<Radar />` is self-contained — it generates its own gradients and glow filter under a unique id, so multiple radars never collide on styles. Compose `<Dot />` and `<ActiveDot />` inside it to add point markers.

<ApiTable>
  <ApiRow name="dataKey" type="string" required>
    The series key to render. Must exist on both the data and the config.
  </ApiRow>
  <ApiRow name="variant" type='"filled" | "lines"' default='"filled"'>
    The visual style for this radar. `"filled"` shows a filled area, `"lines"` shows only the outline.
  </ApiRow>
  <ApiRow name="fillOpacity" type="number" default="0.3">
    The opacity of the filled area when using `variant="filled"`.
  </ApiRow>
  <ApiRow name="isGlowing" type="boolean" default="false">
    Adds a soft outer glow around this radar. Each radar controls its own glow independently.
  </ApiRow>
  <ApiRow name="isClickable" type="boolean" default="false">
    Enables interactive clicking on this radar to select/deselect it. When a radar is selected, unselected radars become semi-transparent.
  </ApiRow>
  <ApiRow name="children" type="ReactNode">
    Optional `<Dot />` and `<ActiveDot />` composition for point markers on this radar.
  </ApiRow>
  <ApiRow name="radarProps" type='Omit<ComponentProps<typeof Radar>, "dataKey">'>
    Additional ECharts options merged into the compiled `option` by NQChart. Read the <Link href="https://echarts.apache.org/en/option.html#series-radar" _blank>ECharts Radar documentation</Link> for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>Dot / ActiveDot</ApiHeading>

Configuration slots composed inside a `<Radar />`. `<Dot />` styles the resting point markers; `<ActiveDot />` styles the hovered/active marker. They render nothing on their own.

<ApiTable>
  <ApiRow name="variant" type='"default" | "border" | "colored-border"'>
    The visual style for the point marker.
  </ApiRow>
</ApiTable>

<ApiHeading>PolarGrid</ApiHeading>

The polar grid lines. Defaults to a dashed polygon grid and maps to ECharts polar grid options.

<ApiTable>
  <ApiRow name="gridType" type='"polygon" | "circle"' default='"polygon"'>
    The shape of the grid lines. `"polygon"` creates angular grid lines, `"circle"` creates circular grid lines.
  </ApiRow>
  <ApiRow name="...props" type="ComponentProps<typeof PolarGrid>">
    Additional polar grid options are merged into the compiled ECharts option. Read the <Link href="https://echarts.apache.org/en/option.html#polar" _blank>ECharts polar documentation</Link> for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>PolarAngleAxis</ApiHeading>

The angular category axis — the labels around the chart's perimeter. Hidden automatically while the chart is loading.

<ApiTable>
  <ApiRow name="dataKey" type="string">
    The key from your data objects to use for the angle axis labels (e.g., categories, skills, months).
  </ApiRow>
  <ApiRow name="...props" type="ComponentProps<typeof PolarAngleAxis>">
    Additional angle axis options are merged into the compiled ECharts option. Read the <Link href="https://echarts.apache.org/en/option.html#angleAxis" _blank>ECharts angleAxis documentation</Link> for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>PolarRadiusAxis</ApiHeading>

The radial value axis — the scale running from the center outward. Hidden automatically while the chart is loading.

<ApiTable>
  <ApiRow name="...props" type="ComponentProps<typeof PolarRadiusAxis>">
    Additional radius axis options are merged into the compiled ECharts option. Read the <Link href="https://echarts.apache.org/en/option.html#radiusAxis" _blank>ECharts radiusAxis documentation</Link> for available props.
  </ApiRow>
</ApiTable>

<ApiHeading>Tooltip</ApiHeading>

The hover tooltip. Reads the chart's selection so its content dims unselected series. Hidden automatically while the chart is loading.

<ApiTable>
  <ApiRow name="variant" type='"default" | "frosted-glass"' default='"default"'>
    Controls the visual style of the tooltip.
  </ApiRow>
  <ApiRow name="roundness" type='"sm" | "md" | "lg" | "xl"' default='"lg"'>
    Controls the border-radius of the tooltip.
  </ApiRow>
  <ApiRow name="defaultIndex" type="number">
    When set, the tooltip will be visible by default at the specified data point index.
  </ApiRow>
</ApiTable>

<ApiHeading>Legend</ApiHeading>

The series legend. When `isClickable` is set, each entry toggles selection of its series. Hidden automatically while the chart is loading.

<ApiTable>
  <ApiRow name="variant" type='"square" | "circle" | "circle-outline" | "rounded-square" | "rounded-square-outline" | …'>
    The visual style variant for the legend indicators.
  </ApiRow>
  <ApiRow name="align" type='"left" | "center" | "right"' default='"center"'>
    Horizontal placement of the legend.
  </ApiRow>
  <ApiRow name="verticalAlign" type='"top" | "middle" | "bottom"' default='"bottom"'>
    Vertical placement of the legend.
  </ApiRow>
  <ApiRow name="isClickable" type="boolean" default="false">
    Lets each legend entry toggle selection of its series, driving the shared selection state read by every `<Radar />`.
  </ApiRow>
</ApiTable>
