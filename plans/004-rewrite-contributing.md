# Plan 004 — Rewrite CONTRIBUTING.md to match the actual project (pnpm + ECharts + registry scripts)

- **Status:** TODO
- **Written against commit:** `f43ccf9` (branch `landing-redesign-and-theme`)
- **Effort:** M · **Risk of change:** low (docs only) · **Priority:** 4

## Why this matters

`CONTRIBUTING.md` describes a project that no longer exists. Verified stale content:

- Line 22/27/110/164/171/176: instructs `yarn install`, `yarn dev`, `yarn gen-cli`, `yarn lint`, `yarn build` — the project uses **pnpm 10.12.1** (`packageManager` field) and has **no `gen-cli` script** at all.
- Line 71: chart example imports `from "recharts"` — the engine was fully migrated to **Apache ECharts** (zero recharts imports remain in `src/`).
- Line 154: "Use `recharts` for chart rendering" — actively wrong guidance.
- Directory structure described (`charts/bar-charts/`, `app/(docs)/`) doesn't match the real layout (`src/registry/echarts-core/`, `src/registry/charts/`, `src/content/docs/`).

A first-time contributor following this document fails at step one. `README.md` is accurate (uses pnpm) — only CONTRIBUTING.md is broken.

## Source of truth (verified facts to write into the new document)

**Toolchain:** pnpm 10.12.1 (enforce via corepack), Node 22 (CI uses it), Next.js App Router, TypeScript strict, Tailwind v4, ESLint 9 flat config, Prettier with `prettier-plugin-sort-imports` + `prettier-plugin-tailwindcss`.

**Actual scripts (from `package.json` — quote these exactly):**

```jsonc
"postinstall": "fumadocs-mdx",                       // generates .source/ (gitignored)
"dev": "next dev",
"build": "pnpm run registry:fresh && next build",
"lint": "eslint",
"registry:build": "pnpm exec tsx ./src/scripts/build-registry.mts",
"registry:clean": "rm -rf public/r && rm -rf registry.json && rm -rf src/registry/__index__.tsx",
"registry:fresh": "pnpm run registry:clean && pnpm run registry:build",
"audit:previews": "node src/scripts/audit-doc-previews.mjs"
```

Typecheck has no script — it's `pnpm exec tsc --noEmit` (what CI runs). If Plan 001 has landed there is also `pnpm test` (Vitest) — check `package.json` before writing and include it only if present.

**Generated-file model (important to explain to contributors):** `src/registry/__index__.tsx` and `registry.json` are generated by `registry:build` but **committed to git**; `public/r/` and `.source/` are generated and **gitignored**. So: fresh clone works immediately, but after adding/renaming a registry item you must run `pnpm run registry:fresh` and commit the regenerated files. `pnpm run audit:previews` verifies docs ↔ registry consistency.

**Real directory layout (verify with `ls` while writing, don't trust this plan blindly):**

```
src/
  app/            # Next.js routes: landing, /docs, /llm/*.md, /llms.txt, /mcp
  components/     # site-internal components (docs UI, landing, shadcn primitives)
  content/docs/   # MDX documentation pages (fumadocs)
  registry/       # THE PRODUCT: installable chart components
    echarts-core/ # engine: compilers, hooks, animation tokens, color resolution
    charts/       # public chart components (NQBarChart, NQPieChart, ...)
    ui/           # chart-adjacent UI shipped via registry (chart.tsx, legend, tooltip)
    examples/     # docs example components
    blocks/       # larger composed blocks
  scripts/        # build-registry.mts, audit-doc-previews.mjs
  lib/            # site lib (llm.ts, agent-docs.ts, registry.ts, source.ts)
```

**CI gates a PR must pass (from `.github/workflows/ci.yml`):** `pnpm audit --audit-level=high`, `pnpm run lint`, `pnpm exec tsc --noEmit`, `pnpm run build` (plus `pnpm test` / `pnpm run audit:previews` if Plan 001 landed).

## Steps

### Step 1 — Read the current file and the references

Read `CONTRIBUTING.md` fully (note its section structure and tone — keep useful generic sections like code-of-conduct pointers, PR etiquette). Read `README.md` to avoid duplicating its content (link to it instead). Run `ls src src/registry src/scripts` to confirm the layout above.

### Step 2 — Rewrite

Replace the body of `CONTRIBUTING.md` with these sections:

1. **Project overview** — one paragraph: shadcn-style chart registry on Apache ECharts; the deliverable is the `src/registry/` source users install via `shadcn add`.
2. **Prerequisites & setup** — Node 22, `corepack enable`, `pnpm install`, `pnpm dev`. Mention postinstall generates `.source/`.
3. **Repository layout** — the tree above, two-line description per dir.
4. **Development workflow** — dev server, where docs MDX lives, how a chart example becomes a docs preview (`ComponentPreview name="..."` ↔ registry `__index__.tsx`).
5. **Adding or changing a chart** — exact checklist: edit/add under `src/registry/`, register in the matching `src/registry/registry-*.ts` manifest, run `pnpm run registry:fresh`, commit regenerated `src/registry/__index__.tsx` + `registry.json`, run `pnpm run audit:previews`, add/update the MDX docs page in `src/content/docs/`. State explicitly: charts use **ECharts** through the engine in `src/registry/echarts-core/` — never import `recharts`.
6. **Quality gates** — the exact CI commands listed above, instruction to run them locally before opening a PR.
7. **Code style** — TypeScript strict, kebab-case filenames, named exports in the registry, Prettier handles import order and class sorting; no `any` in `src/registry/echarts-core/` (currently zero — keep it that way).
8. **PR guidelines** — keep whatever the current file has that's still generic and true (branch naming, commit style) — port it, don't invent new policy.

Keep total length ≤ ~180 lines. Every command in the file must be copy-paste runnable.

### Step 3 — Verify every command you wrote

Run each command that the new CONTRIBUTING.md tells contributors to run (`pnpm install`, `pnpm run lint`, `pnpm exec tsc --noEmit`, `pnpm run registry:fresh`, `pnpm run audit:previews`). Each must exit 0. `pnpm run build` too if time allows. (`pnpm run registry:fresh` regenerates committed files — `git diff` afterward should show no changes if the registry was already fresh; if it shows changes, revert them with `git checkout -- src/registry/__index__.tsx registry.json` and note it.)

## Done criteria (machine-checkable)

1. `rg -i "yarn|recharts|gen-cli" CONTRIBUTING.md` returns **zero** hits.
2. `rg -c "pnpm" CONTRIBUTING.md` ≥ 8.
3. Every fenced `bash` command in the new file exits 0 when run from repo root.
4. `git status` shows only `CONTRIBUTING.md` modified (and nothing in `src/`).

## Scope boundaries

- **In scope:** `CONTRIBUTING.md` only.
- **Out of scope:** `README.md`, `CODE_OF_CONDUCT.md`, `package.json`, any source file, any script. If you find the layout differs from this plan (drift since `f43ccf9`), describe what IS there — never "fix" the code to match the docs.

## Escape hatches — STOP and report if:

- A documented command fails (e.g. `registry:fresh` errors on a clean tree) — that's a real bug worth its own finding; report the output, don't paper over it in the docs.
- CONTRIBUTING.md contains policy sections (CLA, DCO, security disclosure) you can't verify — keep them verbatim and flag them in your report rather than rewriting.

## Maintenance note

This document encodes `package.json` scripts and CI steps by value. Any change to those (e.g. Plan 001 adding `pnpm test`) must update CONTRIBUTING.md in the same PR — consider that a review checklist item.
